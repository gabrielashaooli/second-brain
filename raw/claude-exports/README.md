# raw/claude-exports/

Conversaciones exportadas de **Claude**.

**Dónde exportar:** claude.ai → Settings → Account → *Export data*. Recibirás un
archivo (normalmente `conversations.json` o un `.zip`); déjalo aquí tal cual.

Contenido inmutable: no editar.
